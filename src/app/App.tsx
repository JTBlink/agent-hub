/**
 * Public application entrypoint.
 *
 * The shell implementation lives in AppShell so page modules can be split
 * without changing the bootstrap import used by main.tsx.
 */
export { App } from "./AppShell";
