# Representative Codex user/project configuration. Comments and unknown keys
# must survive raw-text edits.
model = "gpt-5.3-codex"
model_reasoning_effort = "high"
approval_policy = "on-request"
sandbox_mode = "workspace-write"
api_key = "fixture-secret"
future_option = 42

[features]
web_search_request = true
unified_exec = false

[projects."/projects/agent-hub"]
trust_level = "trusted"
